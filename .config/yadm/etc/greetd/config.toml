[terminal]
vt = 1

[default_session]
command = "tuigreet --remember --cmd 'systemd-cat --identifier=sway sway'"
user = "greetd"

# [initial_session]
# command = "systemd-cat --identifier=sway sway"
# user = "nsbg"
